// STEP 1: Create a string variable called petName
const petName = "String";
console.log(petName, typeof petName);

// STEP 2: Create a number variable called heightInCm
const heightInCm = 180;
console.log(heightInCm, typeof heightInCm);

// STEP 3: Create a boolean variable called isStudent
const isStudent = true;
console.log(isStudent, typeof isStudent);

// STEP 4: Create a null variable called lunchTime
const lunchTime = null;
console.log(null, typeof lunchTime);

// STEP 5: Create an undefined variable called favoriteFood
const favoriteFood = undefined;
console.log(favoriteFood, typeof favoriteFood);

// STEP 6: Log the type of each variable using typeof
