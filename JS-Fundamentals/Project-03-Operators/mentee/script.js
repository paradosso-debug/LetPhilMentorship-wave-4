// STEP 1: Use + to add 5 and 3 and store the result in a variable
// Then log the result
const Add = 5 + 3;
console.log(Add);

// STEP 2: Use * to multiply 4 and 2 and store the result in a variable
// Then log the result
const multiply = 4 * 2;
console.log(multiply);

// STEP 3: Use > to compare if 10 is greater than 7 and store the result
// Then log the result
const trueOrFalse = 10 > 7;
console.log(trueOrFalse);

// STEP 4: Use - to subtract 8 from 12 and store the result in a variable
// Then log the result
const subtract = 12 - 8;
console.log(subtract);

// STEP 5: Use / to divide 20 by 4 and store the result in a variable
// Then log the result
const divide = 20 / 4;
console.log(divide);

// STEP 6: Use % to get the remainder of 10 divided by 3 and store it
// Then log the result
const remainder = 10 % 3;
console.log(remainder);

// let number = 10;
// if (number % 2 === 0) {
//   console.log("even");
// } else {
//   console.log("odd");
// }

// STEP 7: Use < to check if 5 is less than 9 and store the result
// Then log the result

const falseOrTrue = 5 < 9;
console.log(falseOrTrue);

// STEP 8: Use == to check if 10 is equal to "10" (loose equality)
// Store the result in a variable and log it

const isEqual = 10 == "10";
console.log(isEqual);

// STEP 9: Use === to check if 5 is strictly equal to "5" (strict equality)
// Store the result in a variable and log it

const isStrictlyEqual = 5 === "5";
console.log(isStrictlyEqual);

// STEP 10: Use !== to check if 7 is not strictly equal to "7"
// Store the result in a variable and log it
const notStrictly = 7 !== "7";
console.log(notStrictly);
